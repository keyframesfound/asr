websocket-client>=1.6.0
sounddevice>=0.4.6
numpy>=1.24.0
python-dotenv>=1.0.0
torch>=2.0.0
transformers>=4.40.0
# scripts/download_models.py (also installed via transformers).
huggingface_hub>=0.23.0
textual>=0.80.0
# SenseVoice (FunASR + fbank backend). Finals are converted with OpenCC s2hk.
funasr>=1.0.0
kaldi-native-fbank>=1.20.0
opencc>=1.1.0
# Parakeet on Apple Silicon (MLX)
parakeet-mlx>=0.5.0
